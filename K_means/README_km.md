START

1. Define dataset with Sepal Length and Petal Length for points P1–P6.

2. Store dataset in a matrix X.

3. Set number of clusters K = 3.

4. Initialize K-Means model with K clusters.

5. Apply K-Means on dataset X.

6. Generate cluster labels for each data point.

7. Find centroids of the clusters.

8. Display table:
      Data Point | Sepal Length | Petal Length | Cluster Label

9. Plot scatter graph using:
      X-axis → Sepal Length
      Y-axis → Petal Length

10. Color data points according to cluster labels.

11. Plot centroids on the graph using a different marker.

12. Display the graph.

END